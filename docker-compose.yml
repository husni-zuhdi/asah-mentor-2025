version: '3.9'

services:
  # web:
  #   image: asah-mentor:latest
  #   env_file:
  #     - ./.env
  #   ports:
  #     - "8081:8081"
  
  postgres:
    image: postgres:14-alpine
    ports:
      - 5432:5432
    volumes:
      - ./apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=password08asd7
      - POSTGRES_USER=asah
      - POSTGRES_DB=asah2025
